services:
  - type: web
    name: crypto-trader-bot
    env: python
    pythonVersion: "3.9.0"
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run trader_bot.py --server.port=$PORT --server.address=0.0.0.0
